"use client";

export default function RadiologyServices() {
    return (
        <div className="p-6">
            <h1 className="text-2xl font-bold mb-4">Radiology Services</h1>
            <p className="text-muted-foreground">Configure the different types of scans and services offered (MRI, CT, X-Ray, etc.).</p>
        </div>
    );
}
